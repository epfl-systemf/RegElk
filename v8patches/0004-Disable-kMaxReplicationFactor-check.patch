From 01c27ddf1ec79368a7a83af625c91d81fbafb76f Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Cl=C3=A9ment=20Pit-Claudel?= <clement.pit-claudel@epfl.ch>
Date: Fri, 3 Nov 2023 15:27:02 +0100
Subject: [PATCH 2/2] Disable kMaxReplicationFactor check

---
 src/regexp/experimental/experimental-compiler.cc | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/regexp/experimental/experimental-compiler.cc b/src/regexp/experimental/experimental-compiler.cc
index 566ae06829f..68094b643f2 100644
--- a/src/regexp/experimental/experimental-compiler.cc
+++ b/src/regexp/experimental/experimental-compiler.cc
@@ -106,7 +106,7 @@ class CanBeHandledVisitor final : private RegExpVisitor {
     // `replication_factor_` keeps track of how often the current node will
     // have to be replicated in the generated bytecode, and we don't allow this
     // to exceed some small value.
-    static constexpr int kMaxReplicationFactor = 16;
+    static constexpr int kMaxReplicationFactor = INT_MAX;
 
     // First we rule out values for min and max that are too big even before
     // taking into account the ambient replication_factor_.  This also guards
-- 
2.34.1

